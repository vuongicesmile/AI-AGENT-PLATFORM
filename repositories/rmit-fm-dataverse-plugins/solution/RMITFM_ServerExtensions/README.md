# RMITFM_ServerExtensions

This segmented solution will contain the plug-in package/assemblies, registered types, steps, images and approved server-side Custom API handlers.

It depends on `RMITFM_DataModel` and must never own or duplicate the project Tables. Build outputs are pipeline artifacts, not committed source.
