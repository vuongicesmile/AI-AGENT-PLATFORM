# Plug-in Integration Tests

Tests that require Dataverse belong here and must target an approved non-production environment. Resolve the target explicitly and never infer production access from a local authentication profile.
