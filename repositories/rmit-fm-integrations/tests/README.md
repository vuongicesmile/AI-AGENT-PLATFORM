# Integration Tests

Organize tests by `unit`, `contract`, `integration`, `reconciliation`, `recovery` and sanitized `fixtures` once projects exist.

Required coverage includes duplicates, missing intervals, stale/out-of-order data, invalid units, late arrivals, timezone/DST behavior, retries, checkpoint recovery, idempotency and record-count/critical-total reconciliation.
