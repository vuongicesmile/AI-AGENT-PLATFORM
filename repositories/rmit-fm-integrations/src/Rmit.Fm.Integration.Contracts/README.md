# Integration Contracts

Define the canonical ingestion envelope, source identifiers, timestamps/timezone evidence, unit/value, quality/status, ingestion run ID, record hash, correlation/batch ID and error contract here after source-to-target mapping approval.

Contracts must be versioned without sharing Dataverse plug-in implementation assemblies across runtime boundaries.
