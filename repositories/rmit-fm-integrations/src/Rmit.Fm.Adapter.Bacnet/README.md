# BACnet Adapter

Implementation is blocked on the vendor-supported access path, BACnet transport/addressing, object map, units/scaling, timestamp ownership and polling limitations.

Never expose BACnet/IP directly to the public internet.
