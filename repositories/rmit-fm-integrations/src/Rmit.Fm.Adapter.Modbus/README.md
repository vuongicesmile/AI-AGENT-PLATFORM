# Modbus Adapter

Implementation is blocked on the Modbus transport variant, device addressing, register map, data types/endianness, units/scaling, timestamp ownership and polling limitations.

Never expose Modbus directly to the public internet.
