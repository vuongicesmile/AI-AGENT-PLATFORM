# Source-to-Target Mappings

Store approved mappings for BMS, PME, Archibus, manual readings and historical migration here. Do not commit production extracts or sensitive source samples.

The utility-cost proof of concept keeps its executable, explicitly unapproved mappings beside the synthetic fixtures at `rmit-fm-integrations/demo/utility-cost-pipeline/config/mappings.json`. See [SPO → Dataverse Utility Cost Demo](../demo/spo-utility-cost-demo.md). Move only reviewed mappings into an approved solution/configuration-data process.
