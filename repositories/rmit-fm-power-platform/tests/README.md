# Power Platform Tests

- `solution/`: pack/import, missing-dependency and component-ownership validation.
- `workflow/`: approve, reject, modify/resubmit, overdue, escalation, duplicate-action and email-failure cases.
- `security/`: positive and negative RBAC tests.

Only sanitized fixtures and non-production identities are allowed.
