# Deployment Settings

Store schemas and sanitized templates only. Do not commit environment URLs, tenant IDs, application IDs, credentials, certificates, connection strings, private endpoints or production connection values.
