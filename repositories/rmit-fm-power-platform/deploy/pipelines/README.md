# Pipeline Definitions

The Git provider and CI/CD platform are not selected. Add Azure Pipelines or GitHub Actions only after the repository host and organizational controls are confirmed.

The eventual pipeline must validate dependencies, build code-first components from source, run tests and Solution Checker, produce immutable artifacts, and retain deployment evidence.
