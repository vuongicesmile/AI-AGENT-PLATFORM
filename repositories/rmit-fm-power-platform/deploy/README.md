# Deployment

This directory contains source-controlled pipeline definitions, non-secret settings templates and cross-repository release manifests.

Managed/unmanaged Solution ZIP files are build artifacts and must not be committed. Test and production writes require the approvals defined in the project instructions.
