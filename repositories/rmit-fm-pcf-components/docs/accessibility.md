# PCF Accessibility Baseline

Every component must provide keyboard navigation, meaningful labels/ARIA attributes, visible focus, accessible contrast, responsive behavior and screen-reader verification appropriate to its host.

CSS must be scoped to the component. Do not access unsupported host DOM or use browser storage for application/offline data.
